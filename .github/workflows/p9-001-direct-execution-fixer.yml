name: P9-001 direct execution fixer

on:
  push:
    branches:
      - ralph/p9-001-rank-sweep
    paths:
      - ".github/workflows/p9-001-direct-execution-fixer.yml"

permissions:
  contents: write

jobs:
  fix-and-validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ralph/p9-001-rank-sweep
          fetch-depth: 0

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Sync frozen environment
        run: uv sync --frozen

      - name: Apply direct-execution implementation
        run: |
          python - <<'PY'
          from pathlib import Path

          def replace_once(path: Path, old: str, new: str) -> None:
              text = path.read_text(encoding="utf-8")
              if text.count(old) != 1:
                  raise SystemExit(f"{path}: expected exactly one replacement target, found {text.count(old)}")
              path.write_text(text.replace(old, new, 1), encoding="utf-8")

          execution = Path("src/tiny_qwen_coder/evaluation/execution.py")
          replace_once(
              execution,
              "import os\nimport shutil\nimport signal\nimport subprocess\nimport tempfile\nimport threading\nimport time\nimport uuid\nfrom collections.abc import Mapping\n",
              "import math\nimport os\nimport resource\nimport shutil\nimport signal\nimport subprocess\nimport sys\nimport tempfile\nimport threading\nimport time\nimport uuid\nfrom collections.abc import Callable, Mapping\n",
          )
          replace_once(
              execution,
              'class OciRuntime(StrEnum):\n    """Supported OCI-compatible command-line runtimes."""\n\n    PODMAN = "podman"\n    DOCKER = "docker"\n',
              'class OciRuntime(StrEnum):\n    """Execution-runtime identifiers persisted in evaluation evidence."""\n\n    PODMAN = "podman"\n    DOCKER = "docker"\n    DIRECT = "direct"\n',
          )
          text = execution.read_text(encoding="utf-8")
          marker = "\n\nclass DirectExecutionHarness(ConstrainedExecutionHarness):"
          if marker in text:
              raise SystemExit("direct execution harness already exists")
          text += r'''


def _write_direct_files(
    root: Path,
    files: tuple[ExecutionFile, ...],
    *,
    max_input_bytes: int,
) -> None:
    total_bytes = sum(len(item.content) for item in files)
    if total_bytes > max_input_bytes:
        raise ExecutionRequestError(
            f"execution input contains {total_bytes} bytes; limit is {max_input_bytes}"
        )
    root.mkdir(mode=0o700)
    for item in files:
        relative = PurePosixPath(item.path)
        destination = root.joinpath(*relative.parts)
        destination.parent.mkdir(parents=True, exist_ok=True, mode=0o700)
        destination.write_bytes(item.content)
        destination.chmod(0o700 if item.executable else 0o600)


def _direct_environment(root: Path) -> dict[str, str]:
    home = root / "home"
    temporary = root / "tmp"
    home.mkdir(mode=0o700)
    temporary.mkdir(mode=0o700)
    python_bin = str(Path(sys.executable).resolve().parent)
    return {
        "HOME": str(home),
        "TMPDIR": str(temporary),
        "PATH": python_bin + os.pathsep + os.defpath,
        "LANG": "C.UTF-8",
        "LC_ALL": "C.UTF-8",
        "PYTHONNOUSERSITE": "1",
        "PYTHONDONTWRITEBYTECODE": "1",
        "PYTHONHASHSEED": "0",
    }


def _direct_limit_setup(
    limits: ExecutionLimits,
    timeout_seconds: float,
) -> Callable[[], None]:
    memory_bytes = limits.memory_mebibytes * 1024 * 1024
    file_bytes = limits.workspace_mebibytes * 1024 * 1024
    cpu_seconds = max(1, math.ceil(timeout_seconds * limits.cpus))

    def apply_limits() -> None:
        os.umask(0o077)
        resource.setrlimit(resource.RLIMIT_CORE, (0, 0))
        resource.setrlimit(resource.RLIMIT_AS, (memory_bytes, memory_bytes))
        resource.setrlimit(resource.RLIMIT_FSIZE, (file_bytes, file_bytes))
        resource.setrlimit(resource.RLIMIT_NOFILE, (limits.open_files, limits.open_files))
        resource.setrlimit(resource.RLIMIT_NPROC, (limits.pids, limits.pids))
        resource.setrlimit(resource.RLIMIT_CPU, (cpu_seconds, cpu_seconds))

    return apply_limits


class DirectExecutionHarness(ConstrainedExecutionHarness):
    """Run generated code directly inside an explicitly accepted outer container boundary.

    This backend intentionally does not claim OCI-equivalent filesystem or network isolation.
    It is never selected implicitly. Callers must opt in with ``allow_reduced_isolation=True``.
    The child still receives a fresh working directory, a credential-free environment, bounded
    input/output, Linux resource limits, a wall-clock timeout, and process-group cleanup.
    """

    def __init__(
        self,
        *,
        temp_root: Path | None = None,
        allow_reduced_isolation: bool = False,
    ) -> None:
        super().__init__(temp_root=temp_root)
        self._allow_reduced_isolation = allow_reduced_isolation

    def run(
        self,
        request: ExecutionRequest,
        execution: ExecutionConfig,
        *,
        limits: ExecutionLimits | None = None,
    ) -> ExecutionResult:
        """Execute one request as a bounded subprocess in the current runner container."""

        if not self._allow_reduced_isolation:
            raise ExecutionHarnessUnavailableError(
                "direct execution requires explicit allow_reduced_isolation=True; "
                "it does not provide OCI-equivalent filesystem or network isolation"
            )
        resolved_limits = limits if limits is not None else ExecutionLimits()

        with tempfile.TemporaryDirectory(
            prefix="tiny-qwen-coder-direct-exec-",
            dir=self._temp_root,
        ) as temporary_root_text:
            temporary_root = Path(temporary_root_text)
            workspace = temporary_root / "workspace"
            _write_direct_files(
                workspace,
                request.files,
                max_input_bytes=resolved_limits.max_input_bytes,
            )
            environment = _direct_environment(temporary_root)

            process: subprocess.Popen[bytes]
            try:
                process = subprocess.Popen(
                    request.command,
                    cwd=workspace,
                    stdin=subprocess.DEVNULL,
                    stdout=subprocess.PIPE,
                    stderr=subprocess.PIPE,
                    env=environment,
                    start_new_session=True,
                    preexec_fn=_direct_limit_setup(
                        resolved_limits,
                        execution.timeout_seconds,
                    ),
                )
            except (OSError, subprocess.SubprocessError) as exc:
                raise ExecutionHarnessUnavailableError(
                    f"could not launch direct execution command {request.command[0]!r}"
                ) from exc

            if process.stdout is None or process.stderr is None:  # pragma: no cover
                _kill_process_group(process)
                raise ExecutionHarnessError("direct execution did not provide output pipes")

            stdout_stream = cast(BinaryIO, process.stdout)
            stderr_stream = cast(BinaryIO, process.stderr)
            stdout_capture = _BoundedCapture.create(resolved_limits.max_output_bytes)
            stderr_capture = _BoundedCapture.create(resolved_limits.max_output_bytes)
            stdout_thread = _start_capture_thread(stdout_stream, stdout_capture)
            stderr_thread = _start_capture_thread(stderr_stream, stderr_capture)
            started = time.monotonic()
            timed_out = False
            try:
                process.wait(timeout=execution.timeout_seconds)
            except subprocess.TimeoutExpired:
                timed_out = True
                _kill_process_group(process)
            finally:
                process.stdout.close()
                process.stderr.close()
                stdout_thread.join(timeout=resolved_limits.cleanup_timeout_seconds)
                stderr_thread.join(timeout=resolved_limits.cleanup_timeout_seconds)

            duration = time.monotonic() - started
            if stdout_thread.is_alive() or stderr_thread.is_alive():
                raise ExecutionHarnessError("direct output capture threads did not terminate cleanly")

            if timed_out:
                status = ExecutionStatus.TIMED_OUT
                exit_code: int | None = None
            else:
                return_code = process.returncode
                if return_code is None:  # pragma: no cover
                    raise ExecutionHarnessError("direct process returned without an exit code")
                exit_code = return_code
                status = ExecutionStatus.SUCCEEDED if exit_code == 0 else ExecutionStatus.FAILED

            return ExecutionResult(
                status=status,
                runtime=OciRuntime.DIRECT,
                exit_code=exit_code,
                duration_seconds=duration,
                stdout=stdout_capture.text(),
                stderr=stderr_capture.text(),
                stdout_truncated=stdout_capture.truncated,
                stderr_truncated=stderr_capture.truncated,
            )
'''
          execution.write_text(text, encoding="utf-8")

          rank_eval = Path("src/tiny_qwen_coder/evaluation/python_rank_sweep.py")
          replace_once(
              rank_eval,
              "    _generate_items,\n    _preflight_execution_images,\n    _preflight_source_tree,\n",
              "    _generate_items,\n    _preflight_source_tree,\n",
          )
          replace_once(
              rank_eval,
              "from tiny_qwen_coder.evaluation.execution import ConstrainedExecutionHarness, discover_oci_runtime\n",
              "from tiny_qwen_coder.evaluation.execution import (\n    ConstrainedExecutionHarness,\n    DirectExecutionHarness,\n)\n",
          )
          replace_once(
              rank_eval,
              "    generation_contract: str,\n    baseline_manifest: object,\n",
              "    generation_contract: str,\n    generation_source_git_sha: str,\n    baseline_manifest: object,\n",
          )
          replace_once(
              rank_eval,
              '        "source_git_sha": source_git_sha,\n        "base_model": asdict(base_model),\n',
              '        "source_git_sha": source_git_sha,\n        "generation_source_git_sha": generation_source_git_sha,\n        "base_model": asdict(base_model),\n',
          )
          old_score = '''def score_rank_stage(
    *,
    rank: int,
    baseline_dir: Path,
    repo_root: Path = Path("."),
) -> Path:
    registry = load_rank_candidate_registry()
    candidate = registry.candidate(rank)
    evaluation, settings, base_model, system_version, system_prompt = _evaluation_context(candidate)
    source_git_sha, _ = _preflight_source_tree(repo_root)
    output_dir = Path(evaluation.output_dir)
    stage = read_json(output_dir / STAGE_MANIFEST, context="P9-001 generation stage")
    adapter = _adapter_from_stage(stage, candidate)
    generation_contract = _adapter_contract(
        base_model=base_model,
        settings=settings,
        system_prompt_version=system_version,
        system_prompt=system_prompt,
        adapter=adapter,
    )
    _validate_stage(
        output_dir=output_dir,
        source_git_sha=source_git_sha,
        candidate=candidate,
        evaluation=evaluation,
        settings=settings,
        system_prompt=system_prompt,
        generation_contract=generation_contract,
        adapter=adapter,
    )
    baseline_manifest, baseline = load_baseline(baseline_dir, base_model)

    runtime = discover_oci_runtime()
    harness = ConstrainedExecutionHarness(runtime=runtime)
    humaneval, he_problems, mbpp, mbpp_problems, holdout = _load_inputs(
        evaluation=evaluation,
        settings=settings,
        base_model=base_model,
        candidate=candidate,
        harness=harness,
    )
    _preflight_execution_images(
        runtime,
        (
            humaneval.runner.execution_image,
            mbpp.runner.execution_image,
            holdout.suite.execution_image,
        ),
    )
'''
          new_score = '''def score_rank_stage(
    *,
    rank: int,
    baseline_dir: Path,
    repo_root: Path = Path("."),
    generation_source_git_sha: str | None = None,
) -> Path:
    registry = load_rank_candidate_registry()
    candidate = registry.candidate(rank)
    evaluation, settings, base_model, system_version, system_prompt = _evaluation_context(candidate)
    scoring_source_git_sha, _ = _preflight_source_tree(repo_root)
    stage_source_git_sha = (
        scoring_source_git_sha
        if generation_source_git_sha is None
        else _git_sha(generation_source_git_sha, context="generation_source_git_sha")
    )
    output_dir = Path(evaluation.output_dir)
    stage = read_json(output_dir / STAGE_MANIFEST, context="P9-001 generation stage")
    adapter = _adapter_from_stage(stage, candidate)
    generation_contract = _adapter_contract(
        base_model=base_model,
        settings=settings,
        system_prompt_version=system_version,
        system_prompt=system_prompt,
        adapter=adapter,
    )
    _validate_stage(
        output_dir=output_dir,
        source_git_sha=stage_source_git_sha,
        candidate=candidate,
        evaluation=evaluation,
        settings=settings,
        system_prompt=system_prompt,
        generation_contract=generation_contract,
        adapter=adapter,
    )
    baseline_manifest, baseline = load_baseline(baseline_dir, base_model)

    harness = DirectExecutionHarness(allow_reduced_isolation=True)
    humaneval, he_problems, mbpp, mbpp_problems, holdout = _load_inputs(
        evaluation=evaluation,
        settings=settings,
        base_model=base_model,
        candidate=candidate,
        harness=harness,
    )
'''
          replace_once(rank_eval, old_score, new_score)
          replace_once(
              rank_eval,
              "            source_git_sha=source_git_sha,\n            candidate=candidate,\n            base_model=base_model,\n            adapter=adapter,\n            settings=settings,\n            generation_contract=generation_contract,\n            baseline_manifest=baseline_manifest,\n",
              "            source_git_sha=scoring_source_git_sha,\n            candidate=candidate,\n            base_model=base_model,\n            adapter=adapter,\n            settings=settings,\n            generation_contract=generation_contract,\n            generation_source_git_sha=stage_source_git_sha,\n            baseline_manifest=baseline_manifest,\n",
          )
          replace_once(
              rank_eval,
              "    return _write_evaluation_manifest(output_dir, source_git_sha, rank)\n\n\ndef verify_rank_evaluation(\n",
              "    return _write_evaluation_manifest(output_dir, scoring_source_git_sha, rank)\n\n\ndef verify_rank_evaluation(\n",
          )
          old_verify_head = '''def verify_rank_evaluation(
    *,
    rank: int,
    baseline_dir: Path,
    repo_root: Path = Path("."),
) -> dict[str, object]:
    registry = load_rank_candidate_registry()
    candidate = registry.candidate(rank)
    evaluation, settings, base_model, system_version, system_prompt = _evaluation_context(candidate)
    source_git_sha, _ = _preflight_source_tree(repo_root)
    output_dir = Path(evaluation.output_dir)
'''
          new_verify_head = '''def verify_rank_evaluation(
    *,
    rank: int,
    baseline_dir: Path,
    repo_root: Path = Path("."),
    generation_source_git_sha: str | None = None,
) -> dict[str, object]:
    registry = load_rank_candidate_registry()
    candidate = registry.candidate(rank)
    evaluation, settings, base_model, system_version, system_prompt = _evaluation_context(candidate)
    scoring_source_git_sha, _ = _preflight_source_tree(repo_root)
    stage_source_git_sha = (
        scoring_source_git_sha
        if generation_source_git_sha is None
        else _git_sha(generation_source_git_sha, context="generation_source_git_sha")
    )
    output_dir = Path(evaluation.output_dir)
'''
          replace_once(rank_eval, old_verify_head, new_verify_head)
          replace_once(
              rank_eval,
              "        source_git_sha=source_git_sha,\n        candidate=candidate,\n        evaluation=evaluation,\n        settings=settings,\n        system_prompt=system_prompt,\n        generation_contract=generation_contract,\n        adapter=adapter,\n    )\n    baseline_manifest, baseline = load_baseline(baseline_dir, base_model)\n",
              "        source_git_sha=stage_source_git_sha,\n        candidate=candidate,\n        evaluation=evaluation,\n        settings=settings,\n        system_prompt=system_prompt,\n        generation_contract=generation_contract,\n        adapter=adapter,\n    )\n    baseline_manifest, baseline = load_baseline(baseline_dir, base_model)\n",
          )
          replace_once(
              rank_eval,
              "        source_git_sha=source_git_sha,\n        candidate=candidate,\n        base_model=base_model,\n        adapter=adapter,\n        settings=settings,\n        generation_contract=generation_contract,\n        baseline_manifest=baseline_manifest,\n",
              "        source_git_sha=scoring_source_git_sha,\n        candidate=candidate,\n        base_model=base_model,\n        adapter=adapter,\n        settings=settings,\n        generation_contract=generation_contract,\n        generation_source_git_sha=stage_source_git_sha,\n        baseline_manifest=baseline_manifest,\n",
          )
          replace_once(
              rank_eval,
              "    expected_manifest = _evaluation_manifest_payload(output_dir, source_git_sha, rank)\n",
              "    expected_manifest = _evaluation_manifest_payload(output_dir, scoring_source_git_sha, rank)\n",
          )
          replace_once(
              rank_eval,
              "        else:\n            item.add_argument(\"--baseline-dir\", type=Path, required=True)\n",
              "        else:\n            item.add_argument(\"--baseline-dir\", type=Path, required=True)\n            item.add_argument(\"--generation-source-git-sha\")\n",
          )
          replace_once(
              rank_eval,
              "                baseline_dir=cast(Path, args.baseline_dir),\n                repo_root=repo_root,\n            )\n",
              "                baseline_dir=cast(Path, args.baseline_dir),\n                repo_root=repo_root,\n                generation_source_git_sha=cast(str | None, args.generation_source_git_sha),\n            )\n",
          )
          replace_once(
              rank_eval,
              "                    baseline_dir=cast(Path, args.baseline_dir),\n                    repo_root=repo_root,\n                ),\n",
              "                    baseline_dir=cast(Path, args.baseline_dir),\n                    repo_root=repo_root,\n                    generation_source_git_sha=cast(\n                        str | None, args.generation_source_git_sha\n                    ),\n                ),\n",
          )

          workflow = Path(".github/workflows/python-p9-rank-sweep-evaluation.yml")
          replace_once(
              workflow,
              "    runs-on: ubuntu-24.04\n    timeout-minutes: 180\n",
              "    runs-on: [self-hosted, linux, x64]\n    timeout-minutes: 180\n",
          )
          replace_once(
              workflow,
              "      EXECUTION_IMAGE: python:3.11.14-slim@sha256:c8271b1f627d0068857dce5b53e14a9558603b527e46f1f901722f935b786a39\n",
              "",
          )
          prepare_block = '''      - name: Prepare constrained execution image
        run: |
          set -euo pipefail
          docker --version
          docker pull "${EXECUTION_IMAGE}"
          docker image inspect "${EXECUTION_IMAGE}" >/dev/null

'''
          replace_once(workflow, prepare_block, "")
          replace_once(
              workflow,
              "      - name: Score transported responses under constrained execution\n",
              "      - name: Score transported responses inside the runner container\n",
          )

          tests = Path("tests/unit/test_constrained_execution.py")
          replace_once(
              tests,
              "    ConstrainedExecutionHarness,\n",
              "    ConstrainedExecutionHarness,\n    DirectExecutionHarness,\n",
          )
          test_text = tests.read_text(encoding="utf-8")
          test_text += r'''


def test_direct_execution_requires_explicit_reduced_isolation_opt_in(tmp_path: Path) -> None:
    request = ExecutionRequest(
        image="fixtures/direct:unused",
        command=("python", "-I", "-B", "runner.py"),
        files=(ExecutionFile.from_text("runner.py", "print('ok')\n"),),
    )

    with pytest.raises(ExecutionHarnessUnavailableError, match="allow_reduced_isolation=True"):
        DirectExecutionHarness(temp_root=tmp_path).run(request, _execution())


def test_direct_execution_scrubs_environment_and_applies_resource_limits(
    tmp_path: Path,
    monkeypatch: pytest.MonkeyPatch,
) -> None:
    monkeypatch.setenv("AWS_SECRET_ACCESS_KEY", "must-not-cross-direct-boundary")
    request = ExecutionRequest(
        image="fixtures/direct:unused",
        command=("python", "-I", "-B", "runner.py"),
        files=(
            ExecutionFile.from_text(
                "runner.py",
                "import json, os, resource\n"
                "print(json.dumps({\n"
                "  'secret': os.environ.get('AWS_SECRET_ACCESS_KEY'),\n"
                "  'cwd': os.getcwd(),\n"
                "  'nofile': resource.getrlimit(resource.RLIMIT_NOFILE),\n"
                "  'core': resource.getrlimit(resource.RLIMIT_CORE),\n"
                "  'as': resource.getrlimit(resource.RLIMIT_AS),\n"
                "  'fsize': resource.getrlimit(resource.RLIMIT_FSIZE),\n"
                "}, sort_keys=True))\n",
            ),
        ),
    )
    limits = ExecutionLimits(
        memory_mebibytes=192,
        pids=23,
        workspace_mebibytes=17,
        open_files=77,
    )

    result = DirectExecutionHarness(
        temp_root=tmp_path,
        allow_reduced_isolation=True,
    ).run(request, _execution(), limits=limits)
    metadata = json.loads(result.stdout)

    assert result.status is ExecutionStatus.SUCCEEDED
    assert result.runtime is OciRuntime.DIRECT
    assert result.exit_code == 0
    assert metadata["secret"] is None
    assert metadata["nofile"] == [77, 77]
    assert metadata["core"] == [0, 0]
    assert metadata["as"] == [192 * 1024 * 1024, 192 * 1024 * 1024]
    assert metadata["fsize"] == [17 * 1024 * 1024, 17 * 1024 * 1024]
    assert not Path(metadata["cwd"]).exists()


def test_direct_execution_timeout_kills_process_group(tmp_path: Path) -> None:
    request = ExecutionRequest(
        image="fixtures/direct:unused",
        command=("python", "-I", "-B", "runner.py"),
        files=(ExecutionFile.from_text("runner.py", "import time\ntime.sleep(60)\n"),),
    )

    result = DirectExecutionHarness(
        temp_root=tmp_path,
        allow_reduced_isolation=True,
    ).run(
        request,
        _execution(timeout_seconds=0.1),
        limits=ExecutionLimits(cleanup_timeout_seconds=1.0),
    )

    assert result.status is ExecutionStatus.TIMED_OUT
    assert result.runtime is OciRuntime.DIRECT
    assert result.exit_code is None
'''
          tests.write_text(test_text, encoding="utf-8")
          PY

      - name: Format changed Python
        run: |
          uv run --frozen ruff format \
            src/tiny_qwen_coder/evaluation/execution.py \
            src/tiny_qwen_coder/evaluation/python_rank_sweep.py \
            tests/unit/test_constrained_execution.py

      - name: Validate targeted implementation
        run: |
          set -euo pipefail
          uv run --frozen ruff check \
            src/tiny_qwen_coder/evaluation/execution.py \
            src/tiny_qwen_coder/evaluation/python_rank_sweep.py \
            tests/unit/test_constrained_execution.py \
            tests/unit/test_python_rank_sweep_evaluation.py
          uv run --frozen mypy \
            src/tiny_qwen_coder/evaluation/execution.py \
            src/tiny_qwen_coder/evaluation/python_rank_sweep.py
          uv run --frozen pytest -q \
            tests/unit/test_constrained_execution.py \
            tests/unit/test_python_rank_sweep_evaluation.py
          git diff --check

      - name: Commit implementation
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            .github/workflows/python-p9-rank-sweep-evaluation.yml \
            src/tiny_qwen_coder/evaluation/execution.py \
            src/tiny_qwen_coder/evaluation/python_rank_sweep.py \
            tests/unit/test_constrained_execution.py
          git diff --cached --check
          git commit -m "feat: support direct P9 benchmark execution"
          git push origin HEAD:ralph/p9-001-rank-sweep
